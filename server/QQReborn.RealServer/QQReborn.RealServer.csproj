<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>QQReborn.RealServer</RootNamespace>
  </PropertyGroup>

  <!-- NapCat local gateway only — no Lagrange.Core / QQReborn.Signing references. -->

</Project>
