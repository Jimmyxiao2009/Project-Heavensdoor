<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>net10.0</TargetFramework>
        <ImplicitUsings>enable</ImplicitUsings>
        <Nullable>enable</Nullable>
        <LangVersion>latest</LangVersion>
        <RootNamespace>QQReborn.Signing</RootNamespace>
    </PropertyGroup>

    <ItemGroup>
        <!-- Shared sign provider for the sign.lagrangecore.org token-auth sec-sign endpoint.
             Used by both _ref/QrTest (standalone diagnostic harness) and QQReborn.RealServer
             (the actual bridge) so the command whitelist never needs syncing in two places again. -->
        <ProjectReference Include="..\..\_ref\LagrangeV2\Lagrange.Core\Lagrange.Core.csproj" />
    </ItemGroup>

</Project>
